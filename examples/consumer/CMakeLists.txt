cmake_minimum_required(VERSION 3.25)
project(shadcn_consumer LANGUAGES CXX)
find_package(shadcn 0.1.0 EXACT REQUIRED COMPONENTS core)
add_executable(consumer main.cpp)
target_link_libraries(consumer PRIVATE shadcn::core)
enable_testing()
add_test(NAME installed_consumer COMMAND consumer)
