# Verify documentation and distribution

Status: planned.

## Dependencies

01-native-verification for initial docs; later component issues for complete coverage.

## Scope

Generate and review the npm lockfile, build Docusaurus and Doxygen, fix broken links and verify public examples. Add reviewed source packaging, dependency notices and package-manager recipes after native gates pass.

## Acceptance

A clean machine can build the library and consume the installed package. The docs build passes. Release metadata describes verified platforms and remaining gaps. Version remains 0.1.0 unless separately authorised.

## Evidence

Build logs, lockfile review, source-package checksums and installation results.

The shared specification, source pin and ownership rules apply. This file is the local tracker record; no GitHub issue has been created.
