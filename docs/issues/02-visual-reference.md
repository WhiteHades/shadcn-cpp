# Build independent browser references

Status: planned.

## Dependencies

01-native-verification.

## Scope

Render the pinned upstream wrappers with locked dependencies and a documented font. Add native per-component captures. Compare every supported state and interrupted animation. Fix missing shadows, invalid/focus rendering and incomplete options.

## Acceptance

Light and dark comparisons cover the selected scales, focus and disabled states. A browser reference is the source of expected output. Record residual differences per component. Real assistive-technology checks run for the first ten controls.

## Evidence

Reference metadata, frame captures, difference reports and reviewed accessibility notes.

The shared specification, source pin and ownership rules apply. This file is the local tracker record; no GitHub issue has been created.
