# Add selection, dates and form recipes

Status: planned.

## Dependencies

04-overlays.

## Scope

Add Select, Native Select, Combobox, Command, Calendar, Date Picker, Input OTP and native form validation recipes. Resolve how source package state maps to Qt before exposing the API.

## Acceptance

Keyboard, pointer, search, locale, date constraints, cancellation, input methods and validation descriptions pass their documented tests.

## Evidence

Source and dependency pins, interaction cases, locale fixtures and reviewed captures.

The shared specification, source pin and ownership rules apply. This file is the local tracker record; no GitHub issue has been created.
