# Add layout and form foundations

Status: planned.

## Dependencies

02-visual-reference.

## Scope

Split into individual issues for Textarea, Toggle, Toggle Group, Radio Group, Slider, Tabs, Accordion, Collapsible, Field, Input Group, Aspect Ratio and basic presentation controls. Read each source and dependency before implementation.

## Acceptance

Each component has a source hash, documented native API, behavioural tests, gallery entry and independent visual evidence. Keyboard and RTL rules match the chosen reference.

## Evidence

Per-component test and review files. Update the catalogue only for components actually completed.

The shared specification, source pin and ownership rules apply. This file is the local tracker record; no GitHub issue has been created.
